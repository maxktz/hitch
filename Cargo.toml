[package]
name = "hitch"
version = "0.0.1-beta.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
inquire = "0.9"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
