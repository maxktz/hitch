{
  "name": "muxi",
  "version": "0.1.0",
  "description": "Give AI agents visibility into your terminal sessions",
  "type": "module",
  "packageManager": "pnpm@10.4.1",
  "bin": {
    "muxi": "./target/release/muxi"
  },
  "scripts": {
    "build": "cargo build --release",
    "preinstall": "node scripts/ensure-pnpm.cjs",
    "prepare": "cargo build --release"
  }
}
